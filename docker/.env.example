# Optional — only required if using the SharedCam status template feature
# (the /status* proxy block in the Caddyfile).
# Create at: HA → Settings → Profile → Security → Long-Lived Access Tokens
HA_TOKEN=
