[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -q

# The pytest-homeassistant-custom-component plugin provides the `hass` fixture
# and other helpers used by Home Assistant integration tests.
