{
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"appPort": [
		"9123:8123"
	],
	"remoteEnv": {
		"PATH": "/workspaces/go2rtc-shared-cam/.homeassistant:${containerEnv:PATH}"
	},
	"postCreateCommand": "bash .devcontainer/scripts/setup_ha_test_env.sh || (for req in $(jq -c -r '.requirements | .[]' custom_components/sharedcam/manifest.json); do python3 -m pip install $req; done); if [ ! -d \".homeassistant\" ] || [ ! -f \".homeassistant/configuration.yaml\" ]; then bash .devcontainer/scripts/setup_homeassistant_runtime.sh; else echo '.homeassistant exists, skipping runtime setup'; fi",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"github.vscode-pull-request-github",
				"ms-python.vscode-pylance"
			],
			"settings": {
				"files.eol": "\n",
				"editor.tabSize": 4,
				"python.pythonPath": "/opt/venv/bin/python",
				"editor.formatOnPaste": false,
				"editor.formatOnSave": true,
				"editor.formatOnType": true,
				"files.trimTrailingWhitespace": true,
				"files.associations": {
					"*.yaml": "home-assistant"
				}
			}
		}
	}
}
